import GoogleAuth from "./googleAuth";

test("signin by id token", async () => {
  const googleSignIn = new GoogleAuth();
  // const idToken = "";
  // const userId = await googleSignIn.signIn(idToken);
  // expect(userId).toBeTruthy();
  // console.log(userId);
});
